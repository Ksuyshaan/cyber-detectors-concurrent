# cyber-detectors-concurrent

---
```markdown
# 🛰️ Практическая работа №8: Сеть кибер-детекторов

> *Многопоточная система анализа данных: сравнение подходов — **ручные потоки**, **ExecutorService**, **CompletionService**, **CyclicBarrier**.*

---

## 🎯 Цель
Освоить параллельное программирование в Java:
- управление потоками,
- синхронизация,
- работа с `Future`, `Callable`, `AtomicLong`,
- профилирование производительности.

---

## ✅ Реализовано
| Этап | Технология | Функция |
|------|-------------|---------|
| 1 | `Thread` + `synchronized` / `AtomicLong` | Ручное управление |
| 2–4 | `ExecutorService` + `Callable<Result>` | Пул потоков, сбор результатов |
| 5 | `ExecutorCompletionService` | Обработка по мере готовности |
| 6 | `CyclicBarrier` | Двухфазная координация |
| 7 | `System.nanoTime()` | Замер и сравнение времени |

- Анализ массива из 100 000 чисел,
- Подсчёт суммы и аномалий (>9000).

---

## 📊 Результаты (пример)
| Метод | Время | Комментарий |
|-------|-------|-------------|
| Ручные потоки | 19 мс | Базовый вариант |
| `ExecutorService` | 5 мс | Оптимальное переиспользование |
| `CompletionService` | 4 мс | Нет простоя — сразу обрабатываем готовые |
| `CyclicBarrier` | — | Гарантирует пошаговую синхронизацию |

---

## Запуск
```bash
mkdir -p bin
javac -d bin src/*.java
java -cp bin CyberDefenseSystem
